# 0.2.0 打磨进度（2026-09-18）

继续自 `2026-09-15-004220-video-subs-020.md`。工作仍在 `claude/video-subtitle-translation-cache-f49d5c` 的独立 worktree；主分支和线上版本未改变。0.2.0 尚未发布，真实接口验证留待后续。

已修复并补回归：读取设置期间取消/换页/换模式的旧启动竞态；旧缓存写入回调影响新视频；分句或原文变化后的译文错配；多标签页共享缓存索引丢项；完整缓存依赖 Key；空译文缓存被误判完整；网络请求无限等待、断流误判跨域以及取消监听器残留；页面桥把 JSON 错误当字幕、漏掉 URL 对象请求、换视频后恢复旧 CC 状态。

界面改动：录播页收起未运行的直播统计，实时翻译保留为次要按钮；缓存显示实际目标语言并提示配置变化；完整缓存无 Key 可看；文字模型名默认留空。两份 README 标明开发版和本地安装方式。

验证：自动化检查包含 30 个 JS 文件语法、10 组自检及 40 个回归用例。离线浏览器验证覆盖 360 像素弹窗、缓存显隐、翻译进度、取消后续翻入口、长错误和设置页。所有网络和字幕数据均为测试替身，未调用真实模型、未验证 YouTube timedtext，也未重新加载用户安装的扩展。

接下来优先在真实 Chrome 验证：字幕获取（人工/自动字幕）、用户实际可用的模型、倍速与拖动同步、自定义接口授权和后台转发、缓存恢复以及跨多次轮换的直播。旧交接中的默认模型名只是当时的占位，不应继续作为默认推荐。

运行 `npm run check`、`npm run package`、`git diff --check`。测试 ZIP 仅含运行文件；临时界面预览文件位于被忽略的 `dist/`，不会打进扩展包。
